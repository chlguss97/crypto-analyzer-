# ── GridBot v4 — Minimal ATR-Adaptive Grid ──

strategy: "grid"

exchange:
  name: "okx"
  symbol: "BTC/USDT:USDT"
  margin_mode: "isolated"

grid:
  enabled: true
  leverage: 10
  size_btc: 0.01
  atr_mult: 0.6
  spacing_min_pct: 0.15
  spacing_max_pct: 0.50
  spacing_type: "geometric"
  atr_period: 14
  atr_timeframe: "5m"

safety:
  circuit_breaker_pct: 2.0
  circuit_breaker_window_sec: 10
  circuit_breaker_freeze_sec: 60
  bot_kill_drawdown_pct: 20

fees:
  maker: 0.0002
  taker: 0.0005

telegram:
  enabled: true

redis:
  host: "localhost"
  port: 6379
  db: 0
